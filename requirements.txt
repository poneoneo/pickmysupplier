streamlit>=1.36
# Exact pin: 0.5.0+ requires Streamlit's newer "components v2" API (needs an
# asset_dir declaration this project doesn't have) and crashes on import — see git history.
streamlit-echarts==0.4.0
numpy>=1.24
pandas>=2.0
sqlmodel>=0.0.16
sqlalchemy>=2.0
rich>=13.0
loguru>=0.7
playwright>=1.40
selectolax>=0.3
python-dotenv>=1.0
groq>=1.0
